import Link from 'next/link'
import { ArrowUpRight, CarFront, MessageSquareText, Plus } from 'lucide-react'
import { vehicles, formatINR } from '@/lib/data'
export default function Admin(){const available=vehicles.filter(v=>v.status==='Available').length; const reserved=vehicles.filter(v=>v.status==='Reserved').length; const value=vehicles.reduce((s,v)=>s+v.price,0); return <main className="p-5 md:p-8 lg:p-10"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[.2em] text-white/35">Overview</p><h1 className="display mt-3 text-5xl md:text-7xl">Good evening.</h1><p className="mt-3 text-sm text-white/45">Your inventory at a glance.</p></div><Link href="/admin/cars/new" className="bg-[#f7f5f0] px-5 py-3 text-sm font-semibold text-[#111]"><Plus className="mr-2 inline h-4 w-4"/> Add vehicle</Link></div><div className="mt-10 grid gap-3 md:grid-cols-3">{[['Available',available,'Live inventory'],['Reserved',reserved,'Held for customers'],['Inventory value',formatINR(value),'Current listing value']].map(([a,b,c])=><div key={a as string} className="border border-white/10 bg-white/[.02] p-6"><p className="text-xs uppercase tracking-[.15em] text-white/35">{a as string}</p><p className="display mt-4 text-4xl">{b as string}</p><p className="mt-2 text-xs text-white/35">{c as string}</p></div>)}</div><div className="mt-10 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><section className="border border-white/10"><div className="flex items-center justify-between border-b border-white/10 p-5"><div><p className="text-xs uppercase tracking-[.15em] text-white/35">Recent inventory</p><h2 className="display mt-2 text-3xl">Cars</h2></div><Link href="/admin/cars" className="text-xs underline underline-offset-4">View all</Link></div><div>{vehicles.slice(0,5).map(v=><div key={v.id} className="flex items-center justify-between border-b border-white/10 px-5 py-4 last:border-0"><div><p className="text-sm font-semibold">{v.brand} {v.model}</p><p className="mt-1 text-xs text-white/35">{v.id} · {v.year} · {v.kilometres.toLocaleString('en-IN')} km</p></div><div className="text-right"><p className="text-sm">{formatINR(v.price)}</p><span className="text-[10px] uppercase tracking-[.15em] text-white/35">{v.status}</span></div></div>)}</div></section><section className="border border-white/10 bg-[#e7e0d4] p-6 text-[#111]"><p className="text-xs uppercase tracking-[.15em] text-black/40">Workflow</p><h2 className="display mt-3 text-4xl">Built for the showroom team.</h2><p className="mt-4 text-sm leading-7 text-black/55">Create a vehicle, add specification data, arrange its gallery, set price, preview and publish. Public inventory remains read-only.</p><div className="mt-7 space-y-4 text-sm">{['Create / edit vehicle','Manage gallery & features','Publish / reserve / sell','Review enquiries'].map((x,i)=><div key={x} className="flex items-center justify-between border-t border-black/10 pt-3"><span>{x}</span><span className="text-xs text-black/35">0{i+1}</span></div>)}</div></section></div></main>}
