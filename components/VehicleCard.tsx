import Link from 'next/link'
import Image from 'next/image'
import type { Vehicle } from '@/lib/data'
import { formatINR } from '@/lib/data'
export default function VehicleCard({v}: {v:Vehicle}){return <Link href={`/cars/${v.slug}`} className="group block"><div className="relative aspect-[4/3] overflow-hidden bg-[#e7e0d4]"><Image src={v.images[0]} alt={`${v.brand} ${v.model} ${v.variant}`} fill sizes="(max-width:768px) 92vw, 32vw" className="object-cover transition duration-700 ease-out group-hover:scale-[1.045]"/><div className="absolute left-3 top-3 bg-[#f7f5f0] px-3 py-1 text-[11px] uppercase tracking-[.15em]">{v.status}</div></div><div className="border-x border-b border-black/10 p-5"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-semibold">{v.brand} {v.model}</h3><p className="mt-1 text-sm text-black/50">{v.variant}</p></div><div className="text-right"><div className="font-semibold">{formatINR(v.price)}</div><div className="mt-1 text-xs text-black/45">{v.year} · {v.kilometres.toLocaleString('en-IN')} km</div></div></div><div className="mt-5 flex flex-wrap gap-x-4 gap-y-2 border-t border-black/10 pt-4 text-xs uppercase tracking-[.1em] text-black/55"><span>{v.fuel}</span><span>{v.transmission}</span><span>{v.body}</span><span>{v.ownership}</span></div></div></Link>}
